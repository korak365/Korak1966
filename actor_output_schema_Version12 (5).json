{
  "actorOutputSchemaVersion": 1,
  "title": "Output of Global Inventory Heatmap",
  "properties": {
    "overview": {
      "type": "string",
      "title": "Inventory dataset",
      "template": "{{links.apiDefaultDatasetUrl}}/items?view=overview"
    },
    "itemCount": {
      "type": "number",
      "title": "Total items collected",
      "template": "{{run.usageInfo.items}}"
    }
  }
}